import React from 'react';
import ProgressBar from './ProgressBar';

const app = () => {
    return (
        <div>
            <ProgressBar progress={70} />
        </div>

    )
}

export default app